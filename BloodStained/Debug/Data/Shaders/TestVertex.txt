#version 150

in vec3 position;

uniform	mat4 projection;
uniform mat4 view;
uniform mat4 model;
uniform float ratio;
uniform float height;
void main()
{
	mat4 vp = projection * view;
	mat4 mvp = vp * model;

    gl_Position = mvp * vec4(position,1.0);
}